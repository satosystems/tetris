{
  "private": true,
  "scripts": {
    "init": "elm init",
    "format": "elm-format src --yes",
    "build": "elm make src/Main.elm --debug --output=./output/index.html",
    "build:release": "elm make src/Main.elm --optimize --output=./output/index.html",
    "reactor": "elm reactor"
  },
  "devDependencies": {
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7"
  }
}
